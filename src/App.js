import React from 'react';
import { hot } from 'react-hot-loader';

const App = () => (
  <div className='App'>
    <h1 className='text-5xl font-bold border h-[100px]'>Hello, World!!!!!</h1>
  </div>
);

export default hot(module)(App);
